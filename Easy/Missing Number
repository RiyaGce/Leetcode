class Solution {
    public int missingNumber(int[] nums)
     {
        int len=nums.length;
       int tot=len*(len+1)/2;
        for(int i=0;i<nums.length;i++)
        {
            tot-=nums[i];
        }
        return tot;
    }
}
