class Solution {
    public int countDigits(int num) 
    {
        int temp=num;
        int count=0;
        while(num!=0)
        {
        int mod= num%10;
        if(temp%mod==0)
        count++;
        num=num/10;
        }
        return count;
    }
}
