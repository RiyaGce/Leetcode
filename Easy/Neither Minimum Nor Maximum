class Solution {
    public int findNonMinOrMax(int[] nums) {
        Arrays.sort(nums);
        int len=nums.length;
        if(len==2||len==1)
        {
        return -1;
        }
        else
        {
        int fin=nums[1];
        return fin;
        }
    }
}
